# Nix
result
result-*

# Artifacts of a manually started darwin.linux-builder VM (the integration
# test keeps its builder state in ~/.cache/reprompt/linux-builder instead)
/keys/
/nixos.qcow2

# direnv
.direnv/

# Python
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.venv/

# Tooling caches
.mypy_cache/
.ruff_cache/
.pytest_cache/

# Editor — regenerated by the flake's shellHook, contains machine-local
# /nix/store paths
.vscode/settings.json

# macOS
.DS_Store

# Claude Code — regenerated by the flake's shellHook, contains
# machine-local /nix/store paths
.claude/settings.local.json

# Serena MCP server state
.serena/
