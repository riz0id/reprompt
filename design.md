
# Guidelines

- Written ASD-STE100 Simplified Technical English (STE) is a controlled language standard.
- Use web search to verify facts.

# Tasks

- [X] Implement an MCP tool proxy which uses an arbitrary Python function to rewrite the incoming MCP JSON that exposes the rewritten rule to the MCP tool sender/requester.
- [X] Implement a new dataclasses.dataclass class that models the MCP request JSON.
